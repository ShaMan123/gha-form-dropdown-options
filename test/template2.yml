name: 🧪 Test Form
title: 🧪 Test Form
body:
  # a comment
  - type: dropdown
    id: _version
    attributes:
      description: Package version
      label: version
      options:
        - x
        - x1
    validations:
      required: true
  - type: dropdown
    id: dropdown
    attributes:
      label: should be cleared
      options:
        -
        - null
    validations:
      required: true
  - type: dropdown
    id: empty
    attributes:
      label: should be cleared
      options:
    validations:
      required: true
  - type: dropdown
    id: static
    attributes:
      label: should be copied to the build form
      options:
        - u
        - v
        -
        - null
    validations:
      required: true
